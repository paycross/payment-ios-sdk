## Summary

What changes, and why.

## Docs

- [ ] Public behaviour changed → the developer portal page is updated (link the api-docs PR)

## Testing

Gates run locally, and what they said. CI runs `swift build` and `swift test` on
Linux and the iOS build and UI tests on macOS; say here what you ran and where.
